# Copy this file to .env and fill values for each environment
# For production, you can prefix variables with PRODUCTION_ (optional)

# General
NODE_ENV=production
PORT=4000

# Security
JWT_SECRET=your_jwt_secret_here_change_this_in_production

# Database (defaults used if DB_* not set)
DB_USER=expediente_user
DB_PASS=MMineria$2017
DB_NAME=expediente_digital
DB_HOST=127.0.0.1
DB_DIALECT=postgres
DB_PORT=5432

# CORS - URLs permitidas en producción
# Frontend producción (si existe)
FRONTEND_URL=https://expediente-frontend.sanjuan.gob.ar

# Laravel producción
LARAVEL_URL=https://edugefinanciero.sanjuan.gob.ar

# NOTA: En desarrollo (NODE_ENV=development), localhost se permite automáticamente
# En producción (NODE_ENV=production), SOLO se permiten las URLs especificadas arriba

# Example per-environment overrides (optional)
# DEVELOPMENT_DB_USER=dev_user
# PRODUCTION_DB_USER=expediente_user
